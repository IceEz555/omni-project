{
  "name": "serial-bridge",
  "version": "1.0.0",
  "description": "Reads data from Serial Port (Arduino) and publishes to MQTT",
  "main": "bridge.js",
  "type": "module",
  "scripts": {
    "start": "node bridge.js"
  },
  "dependencies": {
    "mqtt": "^5.3.4",
    "serialport": "^12.0.0"
  }
}
